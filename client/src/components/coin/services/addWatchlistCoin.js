export default function addWatchlistCoin() {
  try {
    const options = {
      method: "POST",
      headers: {
        "Content-type": "application/json",
      },
      credentials: "include",
    };
  } catch (e) {
    console.log(e);
  }
}
